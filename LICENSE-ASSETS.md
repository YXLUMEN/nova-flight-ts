# 资源授权说明 / Asset Licensing

本项目采用 **分层授权**:程序代码与资源素材适用 **不同**的协议条款.
根目录 [`LICENSE`](/LICENSE)(Apache-2.0)**仅适用于程序代码**, 不适用于任何资源文件.

---

## 一、Apache-2.0 的排除范围

根目录 `LICENSE`(Apache-2.0) 的授权范围 **明确排除**以下内容:

- `resources/**` —— 全部资源目录 (音频、音效、纹理、模型、数据、语言文件等);
- `src-tauri/target/**` —— 构建产物;
- 任何以素材格式存在的文件:`.ogg` `.wav` `.mp3` `.flac` `.png` `.jpg` `.jpeg` `.webp` `.obj` `.gltf` `.glb` `.fbx` 等.

**这些文件的权利不归本项目所有, 不被 Apache-2.0 覆盖, 也不随代码一并授予任何许可.**
资源的具体权利归属见 [`CREDITS.md`](/CREDITS.md) 与 `resources/nova-flight/rights.json`.

> 即, 使用者可以按 Apache-2.0 **使用本项目代码**, 但 **无法获取**其中资源的使用权.
> 资源的使用须另行逐一取得权利人授权.

---

## 二、资源分级

`resources/nova-flight/rights.json` 是 **全量清单**, 列出 `resources/nova-flight/` 下的每一个资源文件. 每个条目以 `tier`
描述权利状态.

| 级别  | `tier` 取值 | 权利状态                                      | 可商用   | 授权                              |
|-------|-------------|-----------------------------------------------|----------|-----------------------------------|
| **Ⅰ** | `owned`     | 版权归原权利人 / 音频作者所有                 | 禁止     | All Rights Reserved, **须先授权** |
| **Ⅱ** | `unowned`   | 本项目**不主张任何权利**, 版权归原权利人      | 禁止     | 仅供非商业同人用途                |
| **Ⅲ** | `public`    | 公有领域 / CC0 等                             | 按原协议 | 保留原协议                        |
| **Ⅳ** | `ai`        | AI 生成, **归属随人类投入/法域/平台条款而定** | 视条款   | 见下文                            |

### Ⅰ 类 · 有所有权资源 (`tier: "owned"`, All Rights Reserved)

- **内容**:音频/音效 (`audios/`、`sounds/`)、归他人所有的美术与数据素材.
- **权利状态**:版权归原权利人 (音频作者等) 所有. 本项目 **不授予**任何使用许可.
- **禁止**:商用、提取、再分发、二次创作后再发布.
- **授权途径**:任何超出"非商业同人引用"的用途, 须事先取得权利人 **书面授权**.
- **登记**:在 `rights.json` 中填 `tier: "owned"`;`author` / `source` / `license`.

### Ⅱ 类 · 无所有权资源 (`tier: "unowned"`, 同人 / 未获授权)

这类资源 **没有明确的所有权或授权**, 属于同人引用. 通过清单逐文件标注, 把"非归属"与"有所有权"显式区分开.

**标注要求:**

1. 在 `rights.json` 中置 `tier: "unowned"`;
2. 在 [`CREDITS.md`](/CREDITS.md) 的"Ⅱ 类"表中同步一条人类可读记录;
3. 该资源相关的任何声明中, **不得**出现"版权所有""Apache-2.0""CC"等暗示本项目
   对其享有权利的措辞;
4. 适用下方统一免责声明.

**统一免责声明 (Ⅱ 类资源通用):**

> 标注为 `tier: "unowned"` 的资源 **不属于本项目所有**, 本项目不主张任何权利,
> 版权归原权利人. 本项目仅在 **非商业同人**范围内引用, 未获权利人授权.
> 禁止商用、禁止再分发、禁止提取用于其他项目.
> 收到权利人通知后, 将在合理期限内移除相关文件.
> 下架/授权联系: _lumengrace@outlook.com_.

### Ⅲ 类 · 公开资源 (`tier: "public"`, 公有领域 / CC0)

- 保留其 **原有协议**, 不得改写为本项目的任何协议;
- 在 `rights.json` 中以 `tier: "public"` 登记来源与原始协议;
- 若原协议要求署名 (如 CC BY), 须在 `CREDITS.md` 中完整保留署名信息.

### Ⅳ 类 · AI 生成资源 (`tier: "ai"`)

AI 生成内容的归属 **不能直接当作 `owned`**:它随 (人类投入程度 × 法域 × 生成平台条款) 变化.

**尽可能记录以下字段:**

| 字段           | 说明                                                                                       |
|----------------|--------------------------------------------------------------------------------------------|
| `generator`    | 生成工具/模型及版本, 如 `suno v6-mini`                                                     |
| `prompt`       | 提示词(或哈希)                                                                             |
| `date`         | 生成日期                                                                                   |
| `humanInput`   | 人类投入程度:`none` / `light` / `substantial`                                              |
| `contributors` | **多作者按角色登记**:`[{ "name": "...",  "role": "prompt \|edit\|direction\|selection" }]` |
| `author`       | 从 contributors 汇总的展示字符串                                                           |
| `toS`          | 生成平台条款(链接或要点)                                                                   |
| `commercial`   | 是否允许商用, **按平台条款判定**, 默认 `false`                                             |

**要点:**

- **平台条款决定能不能用**:免费档常限"个人、非商用", 此时 `commercial` 必须为 `false`;
- **多作者分工**:提示词作者与后期作者分开登记在 `contributors` 里——若二者都有实质投入,
  可能是 **共同作者**, 后续授权须 **全体一致同意**;
- **默认不进商业包**, 除非 `toS` 明确允许商用且人类投入足够高;
- 注意输出与 **训练素材相似** 的侵权风险.

---

## 三、发行时如何筛除资源

**Ⅰ 类 (`owned`) 与 Ⅱ 类 (`unowned`) 都不能商用**, Ⅳ 类 (`ai`) 也默认不能.
因此发行过滤用的是 **白名单**:只有 **明确** 标记为可再分发的条目才放行, 其余一概跳过.

1. 读取 `resources/nova-flight/rights.json`;
2. **只放行**满足任一条件的条目:
    - `tier === "public"`;
    - `tier === "ai"` 且 `toS` 允许商用 (及必要的署名);
    - 任意 `tier` 且显式 `authorized: true`;
3. **其余全部跳过**, 包括:默认的 Ⅰ 类、`unowned`、默认的 `ai`.

---

## 附录 · `rights.json` schema
